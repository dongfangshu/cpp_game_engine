# ai

Agent integrations shipped as skills — running an AI coding agent inside an Xmake project.

| Skill | Topic |
| --- | --- |
| [xmake-harness](../../xmake-harness/) | `xmake ai` — install/setup, sessions, permission modes, non-interactive `--print` |
